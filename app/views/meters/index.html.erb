<div class="display-1">Home</div>

<p class="mt-3">Meter Serial Number</p>

<% @meters.each do |meter| %>
<p class="ms-4">
  <%= link_to(meter.serial, meter_path(meter.id)) %>
</p>  
<% end %>

<%= link_to "New Meter", new_meter_path, class: "mt-4 btn btn-primary"%>